import { Alert, Container } from "react-bootstrap"

const Profile = ()=>{

    return(
        <Container>
            <Alert variant="info">Dashboard</Alert>
        </Container>
    )
}
export default Profile;